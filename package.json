{
  "name": "calculadora-custos",
  "version": "1.0.0",
  "description": "Sistema completo de cálculo de custos para sublimação",
  "main": "backend/server.js",
  "scripts": {
    "start": "echo 'Frontend estático - arquivos servidos via HTTP'",
    "start:backend": "node backend/server.js",
    "backend": "node backend/server.js",
    "frontend": "echo 'Frontend servido via backend'",
    "build": "npm run build:frontend",
    "build:frontend": "node -e \"const fs=require('fs');const path=require('path');if(!fs.existsSync('dist'))fs.mkdirSync('dist');const copyRecursive=(src,dest)=>{const stats=fs.statSync(src);if(stats.isDirectory()){if(!fs.existsSync(dest))fs.mkdirSync(dest);fs.readdirSync(src).forEach(file=>copyRecursive(path.join(src,file),path.join(dest,file)));}else{fs.copyFileSync(src,dest);}};copyRecursive('frontend','dist');console.log('✅ Frontend build completed - files copied to dist/')\"",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "nodemon backend/server.js",
    "dev:frontend": "echo 'Frontend em modo desenvolvimento'"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "helmet": "^7.1.0",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "calculadora",
    "custos",
    "sublimacao",
    "nodejs",
    "express"
  ],
  "author": "Seu Nome",
  "license": "MIT"
}